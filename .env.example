# Copy to .env and fill in. .env is gitignored — never commit real secrets.

# Hugging Face token (for gated models / datasets)
HF_TOKEN=

# Weights & Biases API key (optional experiment tracking)
WANDB_API_KEY=

# Example provider keys for litellm judges (set whichever backend you use)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
